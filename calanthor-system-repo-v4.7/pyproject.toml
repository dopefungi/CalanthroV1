[project]
name = "calanthor-system"
version = "0.2.0"
description = "Calanthor rules kernel + pack library (D&D combat + DC-based procedures)"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
